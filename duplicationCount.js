let text = 'ababcde';
let a = []

let counter = 0;

// converting a string into an array
for (let currentNumber = 0; currentNumber < text.length;currentNumber++){
    // if (text.toLowerCase()[currentNumber] === text.toLowerCase()[currentNumber + 1]){
    //     counter++;
    // }
    a.push(text[currentNumber])

}
// push karega, check karega, reverse karega, pop karega and wapis reverse karega [repeat].

for (let currNum=0;currNum < text.length; currNum++){

    // for (let nxtNum =0;nxtNum < text.length; nxtNum++){
    //     console.log(text[currNum], a[nxtNum + 1])
    //     a.reverse()
    //     a.pop()
    //     a.reverse()
    // }
        
    
}

console.log(a.reverse())